// all reducers starting with the 'identity function' -- fn that returns the same value that is passed in as an argument
export default function (state = null, action) {
  switch (action.type) {
    default:
      return state;
  }
}
